---
interface Props {
    date: Date;
  }

  const { date } = Astro.props;
---

<time class="font-mono post-date" datetime={date.toISOString()}>
  {date.toLocaleDateString("en-us", {
    year: "numeric",
    month: "short",
    day: "numeric",
  })}
</time>
