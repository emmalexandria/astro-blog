---
import Figure from "../Figure.astro";

interface Props {
  src: string;
  alt: string;
}

let {src, alt}: Props = Astro.props 
---

<Figure class="rounded" {src} {alt} caption={alt} loading="lazy"/>
