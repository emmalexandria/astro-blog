---
import Image from "../Image.astro";

interface Props {
  src: string;
  alt: string;
}

let {src, alt}: Props = Astro.props 
---

<Image class="rounded" {src} {alt} caption={alt} loading="lazy"/>
